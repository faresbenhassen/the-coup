#include <SDL/SDL.h>
#include <SDL/SDL_image.h>

/*void pause()
{
// cette fonction permet de mettre en pause le programme
// pour éviter que la fenêtre se ferme aussitôt ouverte
int continuer = 1;
SDL_Event event;
while ( continuer ) {
SDL_WaitEvent(&event);
switch ( event.type ) {
case SDL_QUIT: 
continuer = 0;
break;
default: // ne fait rien
break;
}
}
}
*/
//SDL_FillRect(ecran, NULL, SDL_MapRGB(ecran->format, 255, 0, 0));
//SDL_FillRect(image, NULL, SDL_MapRGB(ecran->format, 255, 0, 0));
